Task:

1.改bug

2.看书

3.休息

学到了一点东西：

1.可以根据请求头来判断是微信还是支付宝等发起的请求

2.将字符串s首字母变为小写：
if(Character.isLowerCase(s.charAt(0))){return s;}
else{s = (new StringBuilder()).append(Character.toLowerCase(s.charAt(0))).append(s.subString(1)).toString();}


明天要用番茄钟